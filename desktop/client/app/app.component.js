import template from './app.html';
import './app.styl';

let appComponent = {
  template,
  restrict: 'E',
  controller: (/*$scope, $element, $attrs, $timeout*/) => {
    //"ngInject";

  }
};

export default appComponent;
